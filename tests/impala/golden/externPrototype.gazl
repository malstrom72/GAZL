; Compiled with Impala version 2.0

; signatures version=1
DEBUG:	! DEFi #1	; signature const DEBUG : int @ externPrototype.impala:20:1
; signature extern native printInt(int n) -> void @ externPrototype.impala:22:1
; signature extern native printFloat(float x) -> void @ externPrototype.impala:23:1
; signature extern native printLF() -> unknown @ externPrototype.impala:24:1

;-----------------------------------------------------------------------------
divmod:	FUNC	; signature func divmod(int a, int b, int-ptr rem) -> int @ externPrototype.impala:26:1
	$q:	OUTi
	$a:	INPi
	$b:	INPi
	$rem:	INPp
;-----------------------------------------------------------------------------
		DIVi $q $a $b	; q = a / b
		DIVi %0 $a $b	; *rem = a - a / b * b
		MULi %0 %0 $b
		SUBi %0 $a %0
		POKE $rem %0 
		RETU   	; }


;-----------------------------------------------------------------------------
main:	FUNC	; signature func main() -> void @ externPrototype.impala:32:1
		PARA *1
	$q:	LOCi
	$r:	LOCi
;-----------------------------------------------------------------------------
		MOVi %1 #17 	; q = divmod(17, 5, &r)
		MOVi %2 #5 
		ADRL %3 $r *0
		CALL &divmod %0 *4	; expects divmod(int, int, int-ptr) -> int @ externPrototype.impala:35:23
		MOVi $q %0 
		MOVi %1 $q 	; printInt(q)
		CALL ^printInt %0 *2	; expects printInt(int) -> void @ externPrototype.impala:36:13
		CALL ^printLF %0 *1	; expects printLF() -> unknown @ externPrototype.impala:36:24
		MOVi %1 $r 	; printInt(r)
		CALL ^printInt %0 *2	; expects printInt(int) -> void @ externPrototype.impala:37:13
		CALL ^printLF %0 *1	; expects printLF() -> unknown @ externPrototype.impala:37:24
		MOVi %1 #42 	; printInt(42)
		CALL ^printInt %0 *2	; expects printInt(int) -> void @ externPrototype.impala:38:14
		CALL ^printLF %0 *1	; expects printLF() -> unknown @ externPrototype.impala:38:25
		MOVf %1 #0.5 	; printFloat(0.5)
		CALL ^printFloat %0 *2	; expects printFloat(float) -> void @ externPrototype.impala:39:17
		CALL ^printLF %0 *1	; expects printLF() -> unknown @ externPrototype.impala:39:28
		RETU   	; }