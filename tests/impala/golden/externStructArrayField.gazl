; Compiled with Impala version 2.0

; signatures version=1
DEBUG:	! DEFi #1	; signature const DEBUG : int @ 14:1
; signature extern struct Grid { n : int, cells : int[], tag : float } @ 16:1
; signature extern native printInt() -> unknown @ 18:1
; signature extern native printFloat() -> unknown @ 19:1
; signature extern native printLF() -> unknown @ 20:1

;-----------------------------------------------------------------------------
sum:	FUNC	; signature func sum(Grid-ptr g) -> int @ 22:1
	$total:	OUTi
	$g:	INPp
	$i:	LOCi
;-----------------------------------------------------------------------------
		MOVi $total #0 	; total = 0
		MOVi $i #0 	; for (i = 0 to 4) total = total + g->cells[i]
		GEQi #0 #4 @.e0
	.l1:	ADDp %0 $g #.o.Grid.cells	; total = total + g->cells[i]
		PEEK %0 %0 $i
		ADDi $total $total %0
		FORi $i #4 @.l1
	.e0:	RETU   	; }


;-----------------------------------------------------------------------------
poke:	FUNC	; signature func poke(Grid-ptr g, int i, int v) -> void @ 29:1
		PARA *1
	$g:	INPp
	$i:	INPi
	$v:	INPi
;-----------------------------------------------------------------------------
		ADDp %0 $g #.o.Grid.cells	; g->cells[i] = v
		POKE %0 $i $v
		RETU   	; }


;-----------------------------------------------------------------------------
main:	FUNC	; signature func main() -> void @ 31:1
		PARA *1
.z.main.raw:	! DEFi #10
	$raw:	LOCA *.z.main.raw
	$g:	LOCp
;-----------------------------------------------------------------------------
		ADRL $g $raw:0 *0	; g = (Grid pointer)&raw[0]
		POKE $g #.o.Grid.n #4	; g->n = 4
		POKE $g #.o.Grid.tag #0.5	; g->tag = 0.5
		MOVp %1 $g 	; poke(g, 0, 10)
		MOVi %2 #0 
		MOVi %3 #10 
		CALL &poke %0 *4	; expects poke(Grid-ptr, int, int) -> void @ 36:16
		MOVp %1 $g 	; poke(g, 1, 20)
		MOVi %2 #1 
		MOVi %3 #20 
		CALL &poke %0 *4	; expects poke(Grid-ptr, int, int) -> void @ 36:32
		MOVp %1 $g 	; poke(g, 2, 30)
		MOVi %2 #2 
		MOVi %3 #30 
		CALL &poke %0 *4	; expects poke(Grid-ptr, int, int) -> void @ 36:48
		MOVp %1 $g 	; poke(g, 3, 40)
		MOVi %2 #3 
		MOVi %3 #40 
		CALL &poke %0 *4	; expects poke(Grid-ptr, int, int) -> void @ 36:64
		MOVp %2 $g 	; printInt(sum(g))
		CALL &sum %1 *2	; expects sum(Grid-ptr) -> int @ 37:17
		CALL ^printInt %0 *2	; expects printInt(int) -> unknown @ 37:18
		CALL ^printLF %0 *1	; expects printLF() -> unknown @ 37:29
		PEEK %1 $g #.o.Grid.n	; printInt(g->n)
		CALL ^printInt %0 *2	; expects printInt(int) -> unknown @ 38:16
		CALL ^printLF %0 *1	; expects printLF() -> unknown @ 38:27
		PEEK %1 $g #.o.Grid.tag	; printFloat(g->tag)
		CALL ^printFloat %0 *2	; expects printFloat(float) -> unknown @ 39:20
		CALL ^printLF %0 *1	; expects printLF() -> unknown @ 39:31
		RETU   	; }